# Firebase Configuration
FIREBASE_PROJECT_ID=demo-wordwise

# For production only (not needed for emulator)
# FIREBASE_SERVICE_ACCOUNT_KEY_PATH=path/to/serviceAccountKey.json

# Development Configuration
USE_FIREBASE_EMULATOR=true
DEV_MODE=true

# CORS Configuration (includes emulator ports)
CORS_ORIGINS=http://localhost:5173,http://localhost:5002,http://localhost:4000

# Server Configuration
PORT=8000
HOST=0.0.0.0

# For production, set USE_FIREBASE_EMULATOR=false and provide service account key 