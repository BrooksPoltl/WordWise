import { Extension } from '@tiptap/core';

export const SuggestionDecorations = Extension.create({
  name: 'suggestionDecorations',

  // TODO: Add logic for applying decorations based on suggestions.
}); 